# S3 Configuration
S3_BUCKET=output
S3_REGION=us-east-1
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=P@ssw0rdminioadmin

# Optional: For S3-compatible services (MinIO, etc.)
# S3_ENDPOINT_URL=https://gpmedia.iribnews.ir

# S3 URL Settings
S3_URL_EXPIRATION=3600
S3_PUBLIC_URLS=false

# Download Settings
MAX_FILE_SIZE_MB=5000
TEMP_DIR=./tmp/jobs
# ALLOWED_HOSTS=youtube.com,vimeo.com
# FFMPEG_PATH=C:\ffmpeg\bin\ffmpeg.exe  # Optional: specify FFmpeg path if not in PATH

# Server Settings
HOST=0.0.0.0
PORT=8000

